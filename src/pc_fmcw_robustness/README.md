# Research planner extensions

This package contains planner-level extensions for robotics under uncertain connectivity. Components are intended to integrate with the existing benchmark without changing the frozen P0-P4 baseline semantics.

Planned modules cover tail-risk objectives, chance constraints, adaptive connectivity weighting, information-channel perturbations, and controlled blackout profiles. Each component is implemented as a reusable primitive so experiments can compose them without duplicating planner logic.
